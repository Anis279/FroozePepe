<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WaterPepe - The Liquidity Meme Legend</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Comic Sans MS', 'Arial', sans-serif;
            background: linear-gradient(135deg, #001a33 0%, #003d5c 50%, #001a33 100%);
            color: #fff;
            overflow-x: hidden;
        }

        /* Animated Water Bubbles Background */
        .bubble-container {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: 0;
            overflow: hidden;
            pointer-events: none;
        }

        .bubble {
            position: absolute;
            bottom: -100px;
            background: radial-gradient(circle, rgba(0, 212, 255, 0.3) 0%, transparent 70%);
            border-radius: 50%;
            animation: rise 15s infinite ease-in;
        }

        @keyframes rise {
            0% {
                bottom: -100px;
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                bottom: 110vh;
                opacity: 0;
            }
        }

        /* Splash Effects */
        .splash {
            position: fixed;
            font-size: 40px;
            animation: splash 2s ease-out;
            pointer-events: none;
            z-index: 1;
        }

        @keyframes splash {
            0% {
                transform: translateY(0) scale(1);
                opacity: 1;
            }
            100% {
                transform: translateY(-200px) scale(0);
                opacity: 0;
            }
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            background: rgba(0, 20, 40, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 3px solid #00d4ff;
            box-shadow: 0 4px 20px rgba(0, 212, 255, 0.3);
        }

        .logo-container {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .logo-img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            border: 3px solid #00d4ff;
            box-shadow: 0 0 20px rgba(0, 212, 255, 0.6);
            animation: logoBounce 2s infinite;
        }

        @keyframes logoBounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .logo-text {
            font-size: 28px;
            font-weight: bold;
            color: #00d4ff;
            text-shadow: 
                0 0 10px rgba(0, 212, 255, 1),
                0 0 20px rgba(0, 212, 255, 0.8),
                2px 2px 0px #003d5c;
            letter-spacing: 2px;
        }

        .nav-links {
            display: flex;
            gap: 25px;
        }

        .nav-links a {
            color: #00d4ff;
            text-decoration: none;
            font-size: 18px;
            font-weight: bold;
            transition: all 0.3s;
            text-shadow: 0 0 5px rgba(0, 212, 255, 0.5);
        }

        .nav-links a:hover {
            color: #fff;
            text-shadow: 0 0 15px rgba(0, 212, 255, 1);
            transform: scale(1.1);
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 120px 20px 50px;
            position: relative;
            z-index: 1;
        }

        .hero-content {
            text-align: center;
            max-width: 1200px;
        }

        .hero h1 {
            font-size: clamp(3rem, 10vw, 7rem);
            font-weight: bold;
            color: #00d4ff;
            text-shadow: 
                0 0 20px rgba(0, 212, 255, 1),
                0 0 40px rgba(0, 212, 255, 0.8),
                4px 4px 0px #003d5c,
                -2px -2px 0px rgba(255, 255, 255, 0.3);
            margin-bottom: 20px;
            letter-spacing: 5px;
            animation: titleWave 3s ease-in-out infinite;
        }

        @keyframes titleWave {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }

        .hero h2 {
            font-size: clamp(1.2rem, 4vw, 2.5rem);
            color: #7dd3fc;
            margin-bottom: 50px;
            font-weight: bold;
            text-shadow: 2px 2px 0px #003d5c;
            animation: subtitlePulse 2s infinite;
        }

        @keyframes subtitlePulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }

        /* WaterPepe Character - Using Actual Image */
        .waterpepe-hero {
            width: clamp(300px, 50vw, 500px);
            height: auto;
            margin: 40px auto;
            filter: drop-shadow(0 0 40px rgba(0, 212, 255, 0.8));
            animation: floatPepe 3s ease-in-out infinite;
            cursor: pointer;
            transition: transform 0.3s;
        }

        .waterpepe-hero:hover {
            transform: scale(1.05) rotate(5deg);
            filter: drop-shadow(0 0 60px rgba(0, 212, 255, 1));
        }

        @keyframes floatPepe {
            0%, 100% { 
                transform: translateY(0px) rotate(0deg); 
            }
            50% { 
                transform: translateY(-30px) rotate(2deg); 
            }
        }

        /* Meme Emoji Decorations */
        .emoji-float {
            position: absolute;
            font-size: 40px;
            animation: emojiFloat 8s infinite;
            opacity: 0.7;
        }

        @keyframes emojiFloat {
            0%, 100% {
                transform: translateY(0) rotate(0deg);
                opacity: 0;
            }
            25% {
                opacity: 0.7;
            }
            75% {
                opacity: 0.7;
            }
            100% {
                transform: translateY(-100vh) rotate(360deg);
                opacity: 0;
            }
        }

        /* CTA Buttons - Meme Style */
        .cta-buttons {
            display: flex;
            gap: 25px;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 40px;
        }

        .btn {
            padding: 20px 50px;
            font-size: 22px;
            border: 4px solid #00d4ff;
            background: linear-gradient(135deg, #00d4ff 0%, #00a3cc 100%);
            color: #fff;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
            text-decoration: none;
            display: inline-block;
            border-radius: 50px;
            box-shadow: 
                0 0 30px rgba(0, 212, 255, 0.6),
                inset 0 0 20px rgba(255, 255, 255, 0.2);
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '🌊';
            position: absolute;
            left: -30px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 30px;
            transition: all 0.3s;
        }

        .btn:hover::before {
            left: 10px;
        }

        .btn:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 
                0 10px 40px rgba(0, 212, 255, 1),
                inset 0 0 30px rgba(255, 255, 255, 0.3);
            background: linear-gradient(135deg, #00ffff 0%, #00d4ff 100%);
        }

        .btn-secondary {
            background: transparent;
            color: #00d4ff;
        }

        .btn-secondary::before {
            content: '💎';
        }

        /* Sections */
        section {
            padding: 100px 20px;
            max-width: 1200px;
            margin: 0 auto;
            position: relative;
            z-index: 1;
        }

        .section-title {
            font-size: clamp(2.5rem, 6vw, 4rem);
            text-align: center;
            margin-bottom: 60px;
            color: #00d4ff;
            text-shadow: 
                0 0 20px rgba(0, 212, 255, 1),
                3px 3px 0px #003d5c;
            letter-spacing: 3px;
            position: relative;
        }

        .section-title::before,
        .section-title::after {
            content: '💧';
            position: absolute;
            font-size: 50px;
            animation: iconBounce 2s infinite;
        }

        .section-title::before {
            left: -60px;
        }

        .section-title::after {
            right: -60px;
            animation-delay: 1s;
        }

        @keyframes iconBounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        /* Meme Card Style */
        .meme-card {
            background: linear-gradient(135deg, 
                rgba(0, 212, 255, 0.2) 0%, 
                rgba(0, 163, 204, 0.3) 100%);
            backdrop-filter: blur(10px);
            border: 4px solid #00d4ff;
            border-radius: 30px;
            padding: 40px;
            margin: 30px 0;
            transition: all 0.3s;
            box-shadow: 
                0 0 30px rgba(0, 212, 255, 0.4),
                inset 0 0 20px rgba(0, 212, 255, 0.1);
            position: relative;
            overflow: hidden;
        }

        .meme-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
            animation: shimmer 3s infinite;
        }

        @keyframes shimmer {
            0% { transform: translate(-50%, -50%) rotate(0deg); }
            100% { transform: translate(-50%, -50%) rotate(360deg); }
        }

        .meme-card:hover {
            transform: translateY(-10px) rotate(1deg);
            border-color: #00ffff;
            box-shadow: 
                0 15px 50px rgba(0, 212, 255, 0.8),
                inset 0 0 30px rgba(0, 212, 255, 0.2);
        }

        /* About Content */
        .about-content {
            font-size: 1.3rem;
            line-height: 1.9;
            color: #b4e0f0;
            text-align: center;
            position: relative;
            z-index: 1;
        }

        .about-content strong {
            color: #00d4ff;
            text-shadow: 0 0 10px rgba(0, 212, 255, 0.8);
            font-size: 1.4rem;
        }

        /* Features Grid - Meme Style */
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
            margin-top: 50px;
        }

        .feature-box {
            text-align: center;
            padding: 30px;
            position: relative;
            z-index: 1;
        }

        .feature-emoji {
            font-size: 80px;
            margin-bottom: 20px;
            display: inline-block;
            animation: emojiSpin 3s infinite;
            filter: drop-shadow(0 0 20px rgba(0, 212, 255, 0.6));
        }

        @keyframes emojiSpin {
            0%, 100% { transform: rotate(0deg) scale(1); }
            50% { transform: rotate(10deg) scale(1.1); }
        }

        .feature-box h3 {
            color: #00d4ff;
            margin-bottom: 15px;
            font-size: 1.8rem;
            text-shadow: 2px 2px 0px #003d5c;
        }

        .feature-box p {
            color: #b4e0f0;
            line-height: 1.7;
            font-size: 1.1rem;
        }

        /* Roadmap - Fun Style */
        .roadmap-container {
            display: flex;
            flex-direction: column;
            gap: 40px;
            margin-top: 50px;
        }

        .roadmap-item {
            display: flex;
            align-items: center;
            gap: 30px;
            position: relative;
            z-index: 1;
        }

        .phase-badge {
            min-width: 120px;
            height: 120px;
            background: linear-gradient(135deg, #00d4ff 0%, #00a3cc 100%);
            border: 5px solid #00ffff;
            border-radius: 50%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            font-weight: bold;
            color: #fff;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            box-shadow: 
                0 0 40px rgba(0, 212, 255, 0.8),
                inset 0 0 20px rgba(255, 255, 255, 0.3);
            animation: phasePulse 2s infinite;
        }

        @keyframes phasePulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .phase-content h3 {
            color: #00d4ff;
            font-size: 2rem;
            margin-bottom: 15px;
            text-shadow: 2px 2px 0px #003d5c;
        }

        .phase-content p {
            color: #b4e0f0;
            line-height: 1.8;
            font-size: 1.2rem;
        }

        /* Community Section */
        .social-grid {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-top: 50px;
            flex-wrap: wrap;
        }

        .social-card {
            width: 150px;
            height: 150px;
            background: linear-gradient(135deg, rgba(0, 212, 255, 0.3) 0%, rgba(0, 163, 204, 0.4) 100%);
            border: 4px solid #00d4ff;
            border-radius: 30px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 70px;
            transition: all 0.3s;
            cursor: pointer;
            box-shadow: 0 0 30px rgba(0, 212, 255, 0.5);
            position: relative;
            overflow: hidden;
        }

        .social-card::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 0%, transparent 70%);
            opacity: 0;
            transition: opacity 0.3s;
        }

        .social-card:hover::before {
            opacity: 1;
        }

        .social-card:hover {
            transform: translateY(-15px) rotate(5deg) scale(1.1);
            border-color: #00ffff;
            box-shadow: 0 20px 60px rgba(0, 212, 255, 1);
        }

        /* Footer - Meme Style */
        footer {
            text-align: center;
            padding: 50px 20px;
            background: rgba(0, 20, 40, 0.95);
            border-top: 4px solid #00d4ff;
            position: relative;
            z-index: 1;
        }

        .footer-pepe {
            width: 80px;
            height: auto;
            margin-bottom: 20px;
            border-radius: 50%;
            border: 3px solid #00d4ff;
            box-shadow: 0 0 30px rgba(0, 212, 255, 0.8);
            animation: footerBounce 2s infinite;
        }

        @keyframes footerBounce {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-15px) rotate(10deg); }
        }

        footer p {
            color: #7dd3fc;
            font-size: 1.1rem;
            margin: 10px 0;
        }

        /* Responsive */
        @media (max-width: 768px) {
            nav {
                padding: 10px 15px;
            }

            .logo-img {
                width: 40px;
                height: 40px;
            }

            .logo-text {
                font-size: 20px;
            }

            .nav-links {
                gap: 15px;
            }

            .nav-links a {
                font-size: 14px;
            }

            .section-title::before,
            .section-title::after {
                display: none;
            }

            .roadmap-item {
                flex-direction: column;
                text-align: center;
            }

            .btn {
                padding: 15px 35px;
                font-size: 18px;
            }
        }
    </style>
</head>
<body>
    <!-- Animated Bubble Background -->
    <div class="bubble-container" id="bubbleContainer"></div>

    <!-- Emoji Floaters -->
    <div id="emojiContainer"></div>

    <!-- Navigation -->
    <nav>
        <div class="logo-container">
            <img src="https://i.postimg.cc/rFfkGZcC/pepe.png" alt="WaterPepe" class="logo-img">
            <div class="logo-text">WATERPEPE</div>
        </div>
        <div class="nav-links">
            <a href="#about">ABOUT</a>
            <a href="#features">VIBES</a>
            <a href="#roadmap">ROADMAP</a>
            <a href="#community">COMMUNITY</a>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>WATERPEPE</h1>
            <h2>🌊 The Liquidity of the Meme Ocean 🌊</h2>
            
            <!-- WaterPepe Character - Actual Image -->
            <img src="https://i.postimg.cc/rFfkGZcC/pepe.png" 
                 alt="WaterPepe" 
                 class="waterpepe-hero"
                 id="mainPepe">
            
            <div class="cta-buttons">
                <a href="#" class="btn">JOIN THE WAVE</a>
                <a href="#" class="btn btn-secondary">DIVE IN</a>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section id="about">
        <h2 class="section-title">BORN FROM THE OCEAN</h2>
        <div class="meme-card">
            <div class="about-content">
                <p>🌊 <strong>WaterPepe</strong> emerged from the depths of the meme ocean, a perfect fusion of <strong>pure liquid energy</strong> and <strong>unstoppable vibes</strong>! 💎</p>
                <br>
                <p>Made entirely of <strong>water and ice</strong>, WaterPepe flows through the crypto space with the power of <strong>a thousand waves</strong>! 🚀</p>
                <br>
                <p>This isn't just another token... This is a <strong>MOVEMENT</strong>. A tide that <strong>cannot be stopped</strong>! Join the current and ride the wave to the moon! 🌙✨</p>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section id="features">
        <h2 class="section-title">THE VIBE CHECK</h2>
        <div class="features-grid">
            <div class="meme-card feature-box">
                <div class="feature-emoji">🌊</div>
                <h3>PURE MEME ENERGY</h3>
                <p>No promises, no stress. Just pure ocean vibes and legendary memes that flow forever!</p>
            </div>
            <div class="meme-card feature-box">
                <div class="feature-emoji">💎</div>
                <h3>DIAMOND HANDS</h3>
                <p>The ocean flows together! WaterPepe is powered by the community, for the community. HODL!</p>
            </div>
            <div class="meme-card feature-box">
                <div class="feature-emoji">🚀</div>
                <h3>TO THE MOON</h3>
                <p>Like water, we adapt. Like waves, we grow. Nothing can contain us. The tide is rising!</p>
            </div>
        </div>
    </section>

    <!-- Roadmap Section -->
    <section id="roadmap">
        <h2 class="section-title">THE MASTER PLAN</h2>
        <div class="roadmap-container">
            <div class="meme-card">
                <div class="roadmap-item">
                    <div class="phase-badge">
                        <div>PHASE</div>
                        <div>1</div>
                    </div>
                    <div class="phase-content">
                        <h3>💧 SPLASH</h3>
                        <p>Launch WaterPepe into the ocean! Build the legendary community. Create the first ripples. Let the memes flow like water!</p>
                    </div>
                </div>
            </div>

            <div class="meme-card">
                <div class="roadmap-item">
                    <div class="phase-badge">
                        <div>PHASE</div>
                        <div>2</div>
                    </div>
                    <div class="phase-content">
                        <h3>🌊 WAVE</h3>
                        <p>Expand the ocean! Partner with fellow water warriors. Tsunami of memes incoming! The wave grows stronger every day!</p>
                    </div>
                </div>
            </div>

            <div class="meme-card">
                <div class="roadmap-item">
                    <div class="phase-badge">
                        <div>PHASE</div>
                        <div>3</div>
                    </div>
                    <div class="phase-content">
                        <h3>🚀 TSUNAMI</h3>
                        <p>Flood the entire space! WaterPepe becomes an unstoppable force! The ocean has spoken. TO THE MOON! 🌙</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Community Section -->
    <section id="community">
        <h2 class="section-title">JOIN THE OCEAN FAM</h2>
        <div class="meme-card">
            <div class="about-content">
                <p>🌊 <strong>WaterPepe thrives on community, memes, and pure aquatic energy!</strong> 💎</p>
                <br>
                <p>Dive in and become part of the wave! Together, we're <strong>UNSTOPPABLE!</strong> 🚀</p>
            </div>
            <div class="social-grid">
                <div class="social-card">𝕏</div>
                <div class="social-card">📱</div>
                <div class="social-card">💬</div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <img src="https://i.postimg.cc/rFfkGZcC/pepe.png" alt="WaterPepe" class="footer-pepe">
        <p>© 2024 WaterPepe. Made with 💧 by the Ocean Gang!</p>
        <p style="margin-top: 15px; font-size: 0.95rem; opacity: 0.8;">🌊 This is a MEME project for fun only! Not financial advice! DYOR! 💎</p>
        <p style="margin-top: 10px; font-size: 1.2rem;">LFG! 🚀🌙</p>
    </footer>

    <script>
        // Create animated bubbles
        const bubbleContainer = document.getElementById('bubbleContainer');
        function createBubble() {
            const bubble = document.createElement('div');
            bubble.className = 'bubble';
            const size = Math.random() * 100 + 50;
            bubble.style.width = size + 'px';
            bubble.style.height = size + 'px';
            bubble.style.left = Math.random() * 100 + '%';
            bubble.style.animationDuration = (Math.random() * 10 + 10) + 's';
            bubble.style.animationDelay = Math.random() * 5 + 's';
            bubbleContainer.appendChild(bubble);
            
            setTimeout(() => bubble.remove(), 20000);
        }

        setInterval(createBubble, 1000);
        for (let i = 0; i < 10; i++) createBubble();

        // Floating emojis
        const emojiContainer = document.getElementById('emojiContainer');
        const emojis = ['💧', '🌊', '💎', '🚀', '🌙', '✨', '💙', '🐸'];
        
        function createEmoji() {
            const emoji = document.createElement('div');
            emoji.className = 'emoji-float';
            emoji.textContent = emojis[Math.floor(Math.random() * emojis.length)];
            emoji.style.left = Math.random() * 100 + '%';
            emoji.style.animationDuration = (Math.random() * 6 + 6) + 's';
            emoji.style.animationDelay = Math.random() * 3 + 's';
            emojiContainer.appendChild(emoji);
            
            setTimeout(() => emoji.remove(), 15000);
        }

        setInterval(createEmoji, 800);

        // Splash effect on main Pepe click
        const mainPepe = document.getElementById('mainPepe');
        mainPepe.addEventListener('click', function(e) {
            const splash = document.createElement('div');
            splash.className = 'splash';
            splash.textContent = '💦';
            splash.style.left = e.clientX + 'px';
            splash.style.top = e.clientY + 'px';
            document.body.appendChild(splash);
            
            setTimeout(() => splash.remove(), 2000);
            
            // Play sound effect (simulated)
            console.log('SPLASH! 💦');
        });

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

        // Button splash effects
        document.querySelectorAll('.btn').forEach(btn => {
            btn.addEventListener('click', function(e) {
                for (let i = 0; i < 5; i++) {
                    setTimeout(() => {
                        const splash = document.createElement('div');
                        splash.className = 'splash';
                        splash.textContent = ['💧', '🌊', '💎'][Math.floor(Math.random() * 3)];
                        splash.style.left = (e.clientX + (Math.random() - 0.5) * 100) + 'px';
                        splash.style.top = (e.clientY + (Math.random() - 0.5) * 100) + 'px';
                        document.body.appendChild(splash);
                        setTimeout(() => splash.remove(), 2000);
                    }, i * 100);
                }
            });
        });

        // Social card interactions
        document.querySelectorAll('.social-card').forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.